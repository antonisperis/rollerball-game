using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ektokseush : MonoBehaviour {

	// Use this for initialization
	void Start () {
       // this.gameObject.transform.Rotate(-0.5f, 0, 0);
    }
	
	// Update is called once per frame
	void Update () {
        
    }
}
